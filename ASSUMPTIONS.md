# ASSUMPTIONS

Running log of product/technical assumptions made without asking. Newest last.

1. **Build tooling**: `electron-vite` is used to bundle main/preload/renderer instead of hand-rolled concurrently+wait-on scripts. It keeps the spec's `electron/main`, `electron/preload` layout via config.
2. **Router**: HashRouter instead of BrowserRouter so routes work under `file://` in a packaged Electron app.
3. **Tailwind 3.x** (not 4.x) for the classic, well-documented config format and stable RTL behavior.
4. **UI kit**: instead of installing shadcn/ui (heavy generator workflow), a small in-repo Radix-style component set is written under `src/components/` with the same look-and-feel goals.
5. **Fonts**: no external font fetching (future classified/offline network). System Hebrew stack: `"Segoe UI", "Assistant", "Rubik", "Heebo", Arial, sans-serif`.
6. **Lint**: ESLint flat config with typescript-eslint recommended + react-hooks. `lint` script exists from checkpoint 0.
7. **IDs/timestamps in mock data** are deterministic literals so tests and demos are reproducible.
8. **Personal numbers** in mock data are obviously fake (7-digit starting 90000xx).
9. **Time model**: all times are `"HH:mm"` strings on a 15-minute grid; dates are `"yyyy-MM-dd"` strings. Timezone-free by design (single-base usage).
10. **Saturday**: default disabled except the configurable 19:15-20:00 post-Shabbat slot; no automatic Shabbat-end calculation (per spec).
11. **Guest-lecture reserve** when no imported data exists: default 2 blocks of 90 minutes in the last week of the training, commander-overridable. Documented in engine.
12. **Version retention**: exactly current published + 2 previous; older versions are dropped silently.
13. **Soldier next-week visibility**: controlled by `allowSoldiersToSeeNextWeek`; default true in mock settings.
14. **Attendance for יום גיבוש**: modeled as a field but UI is a small optional toggle only (future feature per spec).
15. **PDF export**: v1 ships a UI placeholder + print-based export foundation (browser print to PDF of the schedule view); full styled PDF generation is a TODO.
16. **Display mode (תצוגה להקרנה)**: implemented as a full-screen read-only weekly view route.
17. **Excel parsing**: assumes days in columns (ראשון..שבת), first column = times in rows, 15-min or hourly rows; merged cells handled best-effort by forward-filling; cell colors ignored.
18. **Mock "now"**: the app treats the real system date as now; seed trainings are generated relative to today so dashboards always show live-looking data.
19. **Shared-event approval**: approval requests expire when the proposed date passes; expired requests are labeled פג תוקף and do not apply.
20. **Notifications**: in-app only (bell in topbar) backed by the store; no OS notifications in v1.
21. **Support phone**: read from `VITE_SUPPORT_PHONE` (see `.env.example`); UI shows a neutral fallback text if unset.
22. **Environment file**: Vite exposes only `VITE_*` vars to the renderer; all mock-mode flags default to safe values without any .env present.
23. **Month view**: read-only foundation (event chips per day, click opens editor); no drag/drop in month view v1.
24. **Resize by edge**: v1 edits duration via the event modal; visual edge-resize is a TODO (per spec's fallback permission).
25. **Feature-local copy**: strings unique to one feature live in a `copy.ts` inside that feature directory; `src/lib/hebrewCopy.ts` remains the shared dictionary. Same Hebrew-only rule applies.
26. **Shared-change application**: when every commander approves a shared-event change, it is applied to each linked training's DRAFT (not directly to published) and each commander is notified to publish — preserving the draft/publish invariant.
27. **Rejected shared change**: any rejection marks the request "שינוי תקוע" (stuck) and surfaces the pending commanders' phone numbers; there is no auto-retry.
28. **Peak-day overrides**: continuing past the override warning keeps the overlapped events in the draft (as conflicts to resolve via the conflict center) rather than silently deleting them.
29. **Editing locked events**: locked events cannot be dragged; editing them through the event modal is the explicit "override flow" for commanders.
30. **Wizard generation**: the create-training wizard can generate immediately using default reserve values (no imported template yet); import can be attached later and regeneration re-run from the builder.
31. **PDF export v1**: the "ייצוא ל-PDF" button uses the browser/Electron print dialog (print-to-PDF); styled PDF generation is a future TODO.
32. **Publish override**: publishing with BLOCKING conflicts requires ticking an explicit "אישור חריג של המפקד" checkbox in the publish flow (per spec's manual-confirmation requirement).

## Patch 1 — design system, login, soldier polish, calendar (2026-07-08)

33. **Brand**: the app wordmark is **MABALUZ** (Latin, Cherry Bomb One), rendered by `src/components/Wordmark.tsx`. It replaces the old "מב" box + "מה-בלוז" text everywhere it was the product mark (sidebar, login, demo picker, projection, lecturer confirmation, email sender line). All other copy stays Hebrew. `app.name` is now `'MABALUZ'`.
34. **Fonts**: self-hosted via `@fontsource/{assistant,heebo,cherry-bomb-one}` (no CDN, offline-safe), imported in `src/main.tsx`.
35. **Type tiers** (four, fixed, via `.t-display/.t-subhead/.t-body/.t-detail` in `index.css`): Heebo 26/500 · Assistant 20/600 · Assistant 16/400 · Assistant 13/500. `PageHeader`, dashboard `SectionCard`/`EventRow`, and the calendar use them; other feature pages still carry legacy `text-sm/xs` and will be migrated in later per-role passes.
36. **Palette**: app shell is warm mono (`#FFFCF2/#CCC5B9/#403D39/#252422`) + one indigo accent (`#4F46E5`, soft `#E9E7FC`). The five rich colors `#C81D4B/#CC4800/#0F766E/#185FA5/#7209B7` are used ONLY inside calendar blocks (white titles). Token KEYS were kept and only VALUES changed, so most components repainted for free.
37. **Status colors**: danger/warning/success reuse crimson/orange/teal as thin accents (borders, text, icons). `Badge` renders status tones as outline (no fill). Callout `-soft` tokens are whisper-faint 6% tints (kept to avoid a 30-file sweep) — effectively tinted-cream, not saturated fills.
38. **Buttons**: `Button` primary = graphite→indigo skew-wipe (adamgiebl); `publish` variant = dark arrow-slide (Peary74) used for פרסם; `loading` prop shows a spinner and disables (wired on publish/generate-apply/import/reminders). Consistent radius/height across variants. (Bugfix: action-button content is wrapped in a positioned span so the label sits above the `::before` overlay.)
39. **Entry flow**: `/` = cosmetic `LoginScreen` (login/register/forgot tabs; nothing persisted) with a "דלג על ההתחברות" button → `/select-account` (the demo picker, formerly at `/`) → app. Admin area has a "PREVIEW — מסך התחברות" button → `/`.
40. **Soldier role**: label is now "חייל בהכשרה"; FAQ + contact removed from the soldier sidebar; dashboard drops the redundant header button (kept only the in-card link + sidebar); "ההכשרה שלי" is compact with `label - value` rows and "הערות המפקד" beneath it.
41. **Calendar**: two-part blocks (colored header + white detail strip); stronger 2px day dividers; `useNow()` drives a live red now-line + current-block ring; `hideHardIndicators` (soldier) hides lock/eye-off but keeps the shared marker; soldier click-details shows which training a shared block/lecture is shared with.
42. **Drag/drop**: swap-based (`scheduleService.swapDraftEvents`). Dropping a flexible block on another swaps them; the hovered target lifts (translateY + ring). Locked/full-day blocks are fixed; empty-slot drops relocate; occupied-slot drops swap — so blocks can never overlap/stack. Publish keeps the override escape-hatch (unchanged, per user decision).
43. **Grid density**: `PX_PER_HOUR` raised 48→60 for more breathing room.
44. **Deferred to later patches**: applying the four type tiers to every non-soldier page, and per-role dashboard restructuring for commander/senior/admin (this patch did global tokens + the soldier role only).
45. **Palette (patch 3)**: the warm cream shell (`#FFFCF2`) is replaced by a cool light-grey canvas (`#EFF1F6`) with a barely-there indigo wash + faint vertical grade; borders/stone/ink cooled to match. White cards now pop on the grey; the content column is transparent so the canvas shows through. Indigo accent and the five rich block colors are unchanged.
46. **Card textures over generated art**: the disliked indigo card gradient is gone. Rather than baking Magnific-generated raster textures (network/credits, and softer against the offline/classified constraint), each dashboard card carries a crisp hand-built CSS/SVG texture that hints at its content — blueprint grid (my-training), signal dot-field (messages), ruled note-paper (commander notes) — all masked/faded so they stay subtle on white. Swappable for Magnific art later if desired.
47. **Calendar block sizing (patch 3)**: blocks use two fixed sizes only — a one-hour "small" block (horizontal hover-reveal: colour shrinks to the right third, white time panel opens on the left) and a two-hour "large" block (vertical hover-reveal: white time board opens beneath, colour keeps its rounded corners). Because a minimum one-hour height cannot coexist with exact-time positioning for sub-hour back-to-back items without overlap, each day is laid out as a **sequential push-down stack**: a block starts at its true time but is pushed below the previous block when a collision would occur, and sparse gaps re-sync to the axis. The shared hour gutter stays as an approximate reference; exact times show on hover. This honours "minimum one hour / never runs off the grid / breathing room between blocks" at the cost of slight axis drift in crowded runs.
48. **Calendar chrome (patch 3)**: the week picker + colour legend moved out of the grid to the page header (left of the training title / top-left), freeing vertical space; day headers put the date to the left of the day name; gutter stamps get a tick. Time ranges are wrapped `dir="ltr"` so numerics read start→end under RTL.
49. **Navbar (patch 3)**: MABALUZ wordmark enlarged (`nav` size) and seated in a sidebar-width zone so it spans the active nav item; header lifted to `z-30` so the user/bell dropdowns overflow above the content column; the user chip gets a 2px ink border matching its dropdown, the dropdown matches the chip width exactly, the avatar indigo ring is removed, name is 18px/600 ink and role 17px ink-muted (AAA on white); the notification badge is a full-opacity dot emitting small ripple rings that clears when the notifications are viewed.
50. **Hebrew calendar (patch 4)**: added `@hebcal/core` (pure-offline npm — no network at runtime; respects the classified constraint). `src/lib/hebcalendar.ts` renders Hebrew dates (day+month, no nikud), the single most-significant holiday/fast/Rosh-Chodesh label per date (minor markers like Yom Kippur Katan / learning cycles filtered out), Elul detection, and the Israeli summer/winter clock (Friday-before-last-Sunday-of-March → last-Sunday-of-October). Hebrew dates + special-day tags are surfaced on the schedule day-headers and the prayer schedule; the helper is available site-wide for other date surfaces.
51. **Calendar axis rework (patch 4)**: replaced the two-fixed-size / push-down stack with a **true proportional axis + zoom**. A `unit` toggle (ש1 / 15דק׳ / 30דק׳; 15 is the good-looking default) sets a fixed pixel height per unit (`UNIT_PX=54`), so 15-min items render at a readable ~54px and sit at their exact time — the grid scrolls (sticky day-header row). At the hour zoom everything crams to fit the screen (the user's explicit "sacrifice looks for less scrolling" option). Gutter stamps show every unit with on-the-hour in medium weight and sub-hour in light; the top axis line is skipped so it never overruns the first stamp. Projection/display mode keeps the auto-fit behaviour. `fitToScreen` prop removed.
52. **Glassy blocks (patch 4)**: block fill is a translucent tint (≈0.82 top → 0.93 bottom) over `backdrop-blur`, a thin darkened rim, and a sheen overlay; white title is 18px/600 with a text-shadow. Note: the five saturated block colours cannot reach WCAG AAA with white text even opaque, so this targets clear legibility (≈AA) with the requested glass look rather than literal 50% transparency.
53. **Dashboard "now" marker (patch 4)**: the current today-block replaces the corner "עכשיו" pill with a lighter (white/25 over the colour) band carved from the colour block's left edge, aligned with every block's colour-left edge. Today titles are right-aligned (RTL). Card semi-titles are 22px/600 and body text 18px; the three card textures are ~20% stronger and fade along a longer diagonal.
54. **Seed schedules (patch 4)**: `buildBaseWeekEvents` now rotates six deterministic weekday templates (+ two Friday templates) with varied start times and 15m/30m/45m/1–3h durations, so the grid reads like a real, uneven week. Still fully deterministic (rotated by day index) and within 08:00–20:00.
55. **Prayer schedule (patch 4)**: new `/prayers` route + sidebar tab "לו״ז תפילות בבה״ד" for ALL roles (not on any dashboard). `prayerSchedule.ts` derives שחרית / מנחה / ערבית (+ Elul סליחות) per date from the Hebrew calendar and Israeli clock — Thursday and Rosh-Chodesh vary שחרית, summer/winter shift מנחה/ערבית, Saturday has none. Week-navigable.
56. **Block palette (patch 5)**: the rich/dark block colours are replaced by a bright flat set — `#ffbe0b #fb5607 #ff006e #8338ec #3a86ff` widened with same-vibe neighbours (`#06d6a0 #f15bb5 #00bbf9 #9b5de5`). **Flat fills only, no gradients.** Text colour is chosen per fill by relative luminance (`blockTextColor`): light fills (amber, mint, sky) take ink, the rest take white — the fill is never darkened to make white work. Prayer panels reuse blue/orange/purple/mint from the same set.
57. **Textures (patch 5)**: one soft dot-field everywhere. Cards (`.card-tex`) fade it on a long diagonal; dashboard "today" blocks slide it in from the left edge and fade it out by the middle (`.tex-mask-left`); calendar blocks wrap it around the edges with the centre kept clear for the title (`.tex-mask-edges`). Dot colour adapts per block fill (`blockTextureColor`). The glassy sheen gradient is gone.
58. **Calendar axis (patch 5)**: the 15/30/60 zoom toggle is **removed** — the grid is purely an hour axis (with light half-hour stamps and fainter half-hour lines). Blocks keep a readable minimum height (`MIN_BLOCK_H=64`, fits a two-line 18px title plus padding) and are pushed down rather than allowed to overlap, so short items (the 15-min formation) are never clipped; the hour row is floored at 72px so everything has room to breathe.
59. **Dashboard (patch 5)**: the training name moved out of the page subtitle into the "ההכשרה שלי" card as a "שם ההכשרה" row, and the dashboard now fills the viewport (`h-full` flex) with the today-list and the card column scrolling internally — the page itself never scrolls.
60. **Prayer page layout (patch 5)**: day rows take the right half; each row is one long block — day name → Hebrew date → special-day tag, Gregorian date beneath — followed by full-height flat colour panels, each rounded on its left and tucked under the panel to its right (negative inline margin + descending z-index) so each prayer "comes out" from beneath the previous. The left half holds the general prayer instructions, a short summary of the relevant פקודות מטכ״ל for religious soldiers (**explicitly a summary — no order numbers are invented; the binding text lives in the actual orders and should be filled by the unit**), and a generated sunset photo of a soldier praying (`src/assets/images/prayer-sunset.jpg`, baked in — no runtime network).
61. **Textures rework (patch 6)**: two motifs driven by one inline `--tex-fg` var. Dots (`.tex-dots`, `background-position: center` so they sit symmetrically top/bottom + left/right) are used on dashboard "today" blocks (fade from the left edge, `.tex-mask-left`, gone by 66%) and prayer panels (rise from the bottom-left corner, `.tex-mask-corner`, gone by ~33%). Slanted lines (`.tex-lines`, 135° so they run "/") are used on calendar blocks and legend chips (corner-fade 33% on blocks, left-fade 66% on chips). Cards (`.card-tex`) fade their indigo dot-field left→right by 66%. The dashboard "today" card itself is plain white (no texture — the pattern lives only on its colour blocks). Texture opacity bumped for visibility (`blockTextureColor` → 0.18 dark / 0.32 light). Old `--tex-dot` / `.tex-mask-edges` removed.
62. **Calendar blocks (patch 6)**: `MIN_BLOCK_H` lowered 64 → 26 so a true 15-minute item renders ~26px tall (the user accepted "tiny and ugly" over faking an hour). Each block is a white base (`border`, `p-[2px]`) holding the colour panel. Tall blocks (`height ≥ 56`) reveal the time by lifting the colour panel straight **up** on hover (`-translate-y-5`), uncovering the white base, its border, and the time printed beneath; short blocks crossfade the time over the colour (title→0, time→1). No block slides sideways on hover anymore.
63. **Calendar day header (patch 6)**: day name, Gregorian date, a long dash, and the Hebrew date now sit on **one line** (special-day tag beneath). A 7-column week column is only ~150px wide, and the longer Hebrew months (e.g. תמוז) don't fit one line above ~14px — so the header line is a uniform **14px** (day name 600, dates 300), tighter padding (`px-2`), `whitespace-nowrap` + `overflow-hidden`. This is the largest size that keeps every week (incl. Tammuz) on a single unclipped line; projection/display mode stays larger (20px).
64. **Sidebar credit (patch 6)**: the ARTECH text credit is replaced by the unit's SVG wordmark (`src/assets/images/artech-logo.svg`, imported as a URL) with a small "מערך ההדרכה — בה״ד 15" line beneath.
65. **Prayer panels (patch 6)**: prayer name is 18px/600 and the time 18px/400, both centred; the first panel is rounded on its right (start) side too (`rounded-s-2xl`) so the sequence has a clean rounded cap against the white label. On Rosh Chodesh שחרית is labelled **"שחרית ומוסף"** (`prayersCopy.shacharitMusaf`). The פקודות מטכ״ל card was **removed** entirely; the left column is now just the (enlarged, 18px) instructions card with the sunset photo directly beneath it. The `ordersTitle/orders/ordersNote` copy strings remain defined but unused.
66. **Texture motifs revised (patch 7)**: dots are now clean round spheres (`radial-gradient(circle, …)` + wide feather, 17px tile) and lines are **thick** (`repeating-linear-gradient(45deg, …)` → "\" at 3px/13px). `.tex-mask-corner` now fades over 66% (was 33%). All dashboard "today" block dots are a single uniform white (`--tex-fg: rgba(255,255,255,0.2)`) rather than per-colour, and every today-block title is forced **white**. Calendar event blocks and prayer panels both use the thick lines from the bottom-left; the my-schedule legend **card** uses the same lines (indigo, `rgba(79,70,229,0.14)`) while the legend **indicators reverted to plain circular dots** (no pattern).
67. **Dashboard "now" band (patch 7)**: reworked from a carved band into a layered look — the block's base becomes a lighter tint (`tint(color, 0.64)`), the full-colour card sits on top with a left drop-shadow (`marginInlineEnd: 4rem`) so it reads as raised above the band, the pulse dot is **red** (`bg-danger`), and the band itself carries no dots.
68. **Calendar block placement (patch 7)**: `MIN_BLOCK_H` lowered to 18 and the gap-based push-down removed — blocks now sit at their exact proportional top/height and are only nudged on a real time overlap, so back-to-back 15-minute items line up correctly (verified: consecutive blocks have 0 gap, real breaks show a gap). Every block has uniform rounding and a uniform 15px white title hugging the **top-right**; the time reveals bottom-right on hover for tall blocks, crossfades for short ones. Day-header text upscaled to 16px (day 600 / Gregorian 400 / Hebrew 300) — still one unclipped line.
69. **Navbar user chip (patch 7)**: the black `border-2 border-ink` on the chip and on the `AnimatedMenu` dropdown panel became a slight `border border-line` (card-like); the chip is nudged ~5px toward the physical left (`-me-[5px]`).
70. **Prayer rows (patch 7)**: the white day-label now sits **above** the colour panels — it has its own white fill, `z-20`, a left drop-shadow, and the panels tuck under it (`marginInlineStart: -18`, `z-10`); the label widened to 250px with enlarged text (day 20 / Hebrew 16 / Gregorian 15) and the rows column widened via `grid-cols-[1.1fr_0.9fr]`. Prayer name is now 21px/700 (time stays 18px), and panels use the thick line texture. The instructions card's title moved into a tab bar above it (`prayersCopy.instructionsTab = "זמני התפילות בבה״ד"`, room for future tabs). The photo is `flex-1` with an absolutely-filled `<img>` so it fills the leftover height and its bottom lines up exactly with the last (Saturday) prayer row.
71. **Texture fades tuned (patch 8)**: `.tex-mask-corner` fades over **33%** (blocks); a new `.tex-mask-corner-wide` keeps 66% for the legend card. Dashboard "today" dots shifted 2px left (`background-position: calc(50% - 2px) center`). Calendar block lines forced to uniform light white (`--tex-fg: rgba(255,255,255,0.28)`) regardless of fill, so the amber/green blocks now read light like the rest.
72. **Dashboard "now" indicator (patch 8)**: the lighter band was dropped. The whole card now pulses a rose-red halo (`.now-live` → `@keyframes nowPulse`, `#ff006e`, from the block palette) with a solid `border-[#ff006e]`, and a small red pulse dot sits in the colour block's **top-left** (`top-2.5 end-2.5`). The colour block is otherwise normal. The today scroll area got `px-2 py-2` so the first/last cards (and the halo) aren't clipped by the scroll edge.
73. **Calendar blocks stripped (patch 8)**: the white base + grey `border-line` frame + inset were removed — the positioned element **is** the colour block now (flat fill, thin same-hue rim, `rounded-md`). The push-down is gone entirely, so blocks touch or overlap exactly as the times dictate. On hover the time appears **right under the title** for blocks ≥44px, and crossfades over the title for tiny ones. NOTE: with white-only titles, the amber/mint (light) fills have low text contrast — this is the user's explicit "white only" choice, not an oversight.
74. **Prayer page top alignment (patch 8)**: the week-controls line moved out of the page header and into the top of the rows column, so the left column's **folder tab** (`rounded-t-xl` primary-soft, `-mt-px` onto the card so it bulges from the card top) lines up with it. The white day-label is now fully `rounded-2xl`. Prayer-block line texture settles at 33%. The photo (still `flex-1`) grew to fill the now-taller column, bottom still flush with the Saturday row.
75. **Navbar user chip (patch 8)**: nudged a further 3px left → `-me-[8px]`.
76. **Patch 9 tweaks**: dashboard block dots shifted to `calc(50% - 5px)` (3px further left); navbar chip nudged to `-me-[10px]` (2px further left). Calendar event blocks and prayer panels are now **flat** (line texture removed from both); the my-schedule legend card keeps the thick lines but across the **whole field** (no mask). `MIN_BLOCK_H` raised to 22 and tiny blocks vertically centre a single-line (`line-clamp-1`) title with zero vertical padding so a 15-min title fits uncut (verified `titleFits`). On hover the time matches the title **exactly** (15px/600 white): it appears right under the title on blocks ≥44px, and swaps in for the title on tiny ones. The prayer instructions folder tab is now the card's border colour (`bg-line`) with semibold ink text, sized (`py-3`) so the card top lines up with the first prayer row (verified −1px).
77. **No more Electron — pure web app (patch 10)**: the project is now a plain Vite SPA (desktop-oriented). Deleted `electron/` and `electron.vite.config.ts`; `vite.config.ts` is the sole build config (`base: './'` so assets resolve via relative paths — this is what **fixed the prayer photo not loading** in the packaged app; Electron's `file://` + absolute `/assets` paths were the cause). Scripts: `dev` = vite, `build` = typecheck + `vite build` → `dist/`, added `preview`. Removed `electron`/`electron-vite` deps and the `main` field; `tsconfig.node.json` no longer includes `electron`. No `src/` code touched Electron APIs, so app behaviour is unchanged. README updated.
78. **Calendar sizing (patch 10)**: `MIN_HOUR_PX` raised 72 → 88 so a real 15-min block is ~22px — readable **and** its true height, so it never needs inflating (inflation was what made adjacent short blocks cover each other). `MIN_BLOCK_H` back to 18 (safety floor only). Placement stays exact with no push-down. A `TOP_PAD` (12px) spacer under the sticky header keeps the 08:00 stamp visible. Cost: the grid is taller and scrolls internally more.
79. **My-schedule layout (patch 10)**: the sidebar commander-note pop-up was removed; the latest note is now a **stretched card in the page header** between the title (right) and legend (left), clamped to 2 lines, click-to-open a modal with the full text. The page is `flex h-full` so it never scrolls (grid scrolls internally; `SCROLL_RESERVE` → 30). At ~1440 the wide legend wraps to a second row; at ~1920 all three sit on one row.
80. **Prayers layout (patch 10)**: page is `flex h-full` and the 7 day-rows are `flex-1` (min 68px) so the whole screen **fits with no scroll**; the photo is `flex-1 min-h-0` to fill the remainder. The folder tab background changed `bg-line` → `bg-neutral-block` to match the "בראש חודש…" note badge.
81. **Patch 11 tweaks**: navbar chip → `-me-[11px]`; dashboard block dots → `calc(50% - 11px)` and a uniform **80%-white** (`--tex-fg: rgba(255,255,255,0.8)`) so they contrast on every fill, still fading out by 66% via `.tex-mask-left`. My-schedule legend split into **two right-aligned rows** (`Math.ceil(n/2)` split), vertically centred in its (stretched) card. Prayer name weight 700 → 600.
82. **Calendar block text never breaks (patch 11)**: block content is vertically centred and the title is clamped to the exact number of lines that fit (`WebkitLineClamp = floor((height−3)/19)`, capped 3); when there's a spare line the time takes it (revealed on hover), otherwise the time swaps in for the title. Verified via a DOM sweep over all block heights (22/44/66/88/132/176/220/264px) — **0 clipped**.
83. **Reminder tests made deterministic (patch 11)**: `tests/reminders.test.ts` cases 1–2 now pass an explicit `reminderNow()` (tomorrow 06:00, 4h before the seeded 10:00 lecture) to `runReminderScheduler`, instead of the real clock — the 24h window otherwise only opened after 10:00 local, so the suite failed when run before 10:00. Not a product bug; the scheduler already accepted `now`.
